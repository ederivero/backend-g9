// Cuando nosotros queremos utilizar el formato de ECMAscript la forma en la que node reconoce que utilizaremos esa forma de trabajar sera mediante la extension del archivo
export const edad = 30;
export const nacionalidad = "Peruano";

export function saludar(nombre) {
  return nombre;
}
